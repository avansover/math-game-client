export enum ExcersizeType {
    PLUS = '+',
    MINUS_POSITIVE = '-',
    MULTIPLY = 'x',
    DIVIDE = '/'
}

export enum CalcAction {
    PLUS = '+',
    MINUS = '-',
    MULTIPLY = 'x',
    DIVIDE = '/'
}

export enum IntRange {
    TEN = 10,
    TWENTY = 20,
    FIFTY = 50,
    ONE_HUNDERD = 100
}

export enum Page {
    MAIN_MENU = "MainMenu",
    POP_UP = "PopUp",
    
}